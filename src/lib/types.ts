export type CardType = {
  backgroundUrl: string;
  name: string;
  description: string;
  date: string;
  time: string;
  location: string;
  QRCode: string; // might change to an image URL later
};
